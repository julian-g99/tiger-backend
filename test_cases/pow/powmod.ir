#start_function
int pow(int x, int n):
int-list: r, t, s, t0, t1, t2
float-list:
    brneq, LABEL0, n, 0
    assign, r, 1
    goto, RET
LABEL0:
    div, t, n, 2
    callr, s, pow, x, t
    mult, s, s, s
    assign, r, s
    mult, t, t, 2
    breq, RET, t, n
    mult, s, s, x
    assign, r, s
RET:
    return, r
#end_function

#start_function
void main():
int-list: a, b, c
float-list:
    assign, a, 8
    assign, b, 1
    brlt, END, b, 0
    callr, c, pow, a, b
    call, puti, c
    call, putc, 10
END:
#end_function
